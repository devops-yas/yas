write:
  replicas: 1
read:
  replicas: 1
backend:
  replicas: 1
loki:
  useTestSchema: true
  commonConfig:
    replication_factor: 1
  auth_enabled: false
test:
  enabled: false
storage:
  type: 'filesystem'
minio:
  enabled: true

monitoring:
  lokiCanary:
    enabled: false
  selfMonitoring:
    enabled: false
    grafanaAgent:
      installOperator: false

chunksCache:
  enabled: false

lokiCanary:
  enabled: false